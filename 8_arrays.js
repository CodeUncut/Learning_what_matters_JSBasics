//array - hum ek variable me ek hi value store karwa sakte hai, per jab hume ek ya usse jyada values store karwani hoti hai, tab hum array use karte hai matlab ki array aapko freedom deta hai ek ya usse jyada values store karwane ki

// array = group of values

// var a = [12,13];
// console.log(a);


//array counting starts from 0

// var arr = [1,2,3,4,5,6];
// console.log(arr[1])


// - push pop shift unshift splice

arr = [1,2,3,4,5,6,7]
// arr.push(8) // adds 8 at the end of the array 
// arr.pop() // removes last value from the array
// arr.unshift(8) //adds 8 at the start of the array
// arr.shift() // removes first value from the array

// arr.splice(2)
// arr.splice(2,1) // it is used to remove value from the middle of an array (index no., how many value you have to remove)
console.log(arr)